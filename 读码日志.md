第一条链路：学生询问智能回答的链路

用户前端输入问题——>后端qacontler——>service.ask方法，

1、BM25的检索（通过课程的id筛选），BM25检索1.1全文索引的筛选，有结果进行下一步，没结果全量加载，1.2预处理：切片分词+统计词频，查询词没查到返回空列表，1.3计算每条切片的得分。1.4降序排序，过滤低分，取Top-k默认5，返回

2.开始拼接知识库的上下文，滑动窗口截断，阈值为2000token，获取最近对话历史

3.模型调用，首先判断是否开启模型调用，3.1未开启走检索，返回检索结果走到落库   .3.2开启检索，判断知识库信息是否有检索到，没有就把问题和上下文给llm让他回答，3.3有知识库，把上下文，问题，知识库给大模型。3.4统计大模型调用时间和是否调用失败，成功就返回成功，失败降级并且标记未走检索模式，如果没有检索到知识库就返回系统设置“未找到课程知识库相关资料”

4.落库，成功和不成功都落库，字段包含用户id，课程id，问题，答案，检索资料json（前端展示资料来源，溯源）、引用切片的id、提问问题时间，回答耗时。

5.封装信息响应给前端

